





export type AudioRecorderConfig = {
    sampleRate: number
    channels: number
    bitsPerSample: number
    audioSource: number
    wavFile: string
}

export type TunerConfig = {
    sampleRate: number
    channels: number
    bitsPerSample: number
    audioSource: number
    wavFile: string
}